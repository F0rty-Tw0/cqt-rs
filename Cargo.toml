[package]
name = "cqt-rs"
version = "0.2.1"
edition = "2024"
rust-version = "1.88"
authors = ["Artiom Tofan <artiomtofancv@gmail.com>"]
description = "Pitch-aligned spectrogram: fast constant-Q and variable-Q transform for music analysis, audio fingerprinting and real-time audio"
documentation = "https://docs.rs/cqt-rs"
readme = "README.md"
license = "MIT"
repository = "https://github.com/F0rty-Tw0/cqt-rs"
keywords = ["cqt", "spectrogram", "music", "audio", "dsp"]
categories = ["multimedia::audio", "science", "mathematics"]
exclude = ["/.github/**", "/.gitignore", "/plots/**", "/docs/**", "/scripts/**"]

[lib]
name = "cqt_rs"
path = "src/lib.rs"

[features]
default = ["parallel"]
# Process batch frames on a rayon thread pool.
parallel = ["dep:rayon", "halfband-rs/parallel"]

[dependencies]
halfband-rs = "0.1.1"
hann-rs = "0.2.1"
ndarray = { version = "0.17", default-features = false, features = ["std"] }
rayon = { version = "1.10", optional = true }
realfft = "3.5"
rustfft = "6.4"
thiserror = "2"

[dev-dependencies]
approx = "0.5"
criterion = "0.8"
hound = "3.5"
plotters = { version = "0.3.7", default-features = false, features = [
  "svg_backend",
  "line_series",
] }

[[bench]]
name = "bench"
harness = false

[lints.rust]
unsafe_code = "forbid"

[profile.bench]
debug = false
